apiVersion: v1
kind: Service
metadata:
  name: pharos-docs-<%= var.git_branch %>
  namespace: kontena-io
spec:
  ports:
     -  port: 80
        protocol: TCP
        targetPort: 80
  selector:
    app: pharos-docs-<%= var.git_branch %>